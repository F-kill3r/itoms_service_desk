version: "4"
services:
  mysql:
    image: mysql:8.3.0
    container_name: mysql
    environment:
      MYSQL_ROOT_PASSWORD: mysql
      MYSQL_DATABASE: service_desk
    ports:
      - "3306:3306"
    volumes:
      - ./mysql:/var/lib/mysql

